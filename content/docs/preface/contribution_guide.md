---
summary: 为 AdonisJS 项目做贡献是回馈社区的好方法。本指南提供了如何为任何 AdonisJS 项目做贡献的概述。
---

# 贡献

这是所有 [AdonisJS](https://github.com/adonisjs) 仓库的通用贡献指南。在向任何仓库贡献之前，请完整阅读本指南 🙏

代码不是唯一的贡献方式。以下是一些贡献方式并成为社区一部分的方法：

- 修复文档中的拼写错误
- 改进现有文档
- 编写教程或博客文章来教育社区中的其他人
- 处理问题分类
- 在现有问题上分享你的观点
- 在 [discord](https://discord.gg/vDcEjq6) 和讨论论坛中帮助社区。

## 报告错误

在开源项目中报告的许多问题通常是提问者端的问题或配置错误。因此，我们强烈建议你在报告问题之前正确排查你的问题。

如果你要报告错误，请尽可能多地提供信息和代码示例。从好到差的议题标准如下：

- **完美议题**：你隔离了底层错误。在仓库中创建一个失败的测试，并围绕它打开一个 Github 议题。
- **好议题**：你隔离了底层错误，并提供一个最小化的复现作为 Github 仓库。Antfu 写了一篇很棒的文章 [为什么需要复现](https://antfu.me/posts/why-reproductions-are-required)。
- **一般议题**：你正确陈述了你的问题。分享首先产生问题的代码。同时包含相关的配置文件和你使用的包版本。

  最后但同样重要的是，通过遵循 [Github markdown 语法指南](https://docs.github.com/en/get-started/writing-on-github/getting-started-with-writing-and-formatting-on-github/basic-writing-and-formatting-syntax) 正确格式化每个代码块。

- **差议题**：你倾倒了你遇到的问题，希望其他人会问相关问题并帮助你。这类议题会在没有任何解释的情况下自动关闭。

## 进行讨论

你经常想要讨论一个话题，或者可能分享一些想法。在这种情况下，在讨论论坛的 **💡想法** 类别下创建一个讨论。

## 教育他人

教育他人是为任何社区做贡献并赢得认可的最佳方式之一。

你可以使用我们讨论论坛上的 **📚 教程** 类别与他人分享文章。教程部分不是严格审核的，除了共享的知识应该与项目相关。

## 创建拉取请求

在投入大量时间和精力编写代码后，拉取请求被拒绝绝不是好的体验。因此，我们强烈建议你在开始任何新工作之前先 [发起讨论](https://github.com/orgs/adonisjs/discussions)。

只需开始一个讨论并解释你计划贡献什么？

- **你是否试图创建一个 PR 来修复错误**：一旦错误被确认，修复错误的 PR 通常会被接受。
- **你是否计划添加新功能**：请彻底解释为什么需要这个功能，并分享我们可以阅读的学习材料链接来教育我们自己。

  例如：如果你正在为 Japa 或 AdonisJS 添加快照测试支持。那么分享我可以用来了解更多关于快照测试的链接。

> 注意：你还应该可以打开额外的 PR 来记录贡献的功能或改进。

## 仓库设置

1. 首先在本地机器上克隆仓库。

   ```sh
   git clone <REPO_URL>
   ```

2. 在本地安装依赖。请不要随功能请求一起更新任何依赖。如果你发现过时的依赖，创建一个单独的 PR 来更新它们。

   我们使用 `npm` 管理依赖，因此不要使用 `yarn` 或任何其他工具。

   ```sh
   npm install
   ```

3. 通过执行以下命令运行测试。

   ```sh
   npm test
   ```

## 使用的工具

以下是使用的工具列表。

| 工具                   | 用途                                                                                                                                                                                                      |
| ---------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| TypeScript             | 所有仓库都使用 TypeScript 编写。编译后的 JavaScript 和类型定义发布在 npm 上。                                                                                                                             |
| TS Node                | 我们使用 [ts-node](https://typestrong.org/ts-node/) 在不编译 TypeScript 的情况下运行测试或脚本。ts-node 的主要目标是在开发过程中有更快的反馈循环                                                          |
| SWC                    | [SWC](https://swc.rs/) 是一个基于 Rust 的 TypeScript 编译器。TS Node 为使用 SWC 而不是 TypeScript 官方编译器提供了一流的支持。使用 SWC 的主要原因是速度提升。                                             |
| Release-It             | 我们使用 [release-it](https://github.com/release-it/release-it) 在 npm 上发布我们的包。它完成了创建发布并在 npm 和 Github 上发布的所有繁重工作。其配置在 `package.json` 文件中定义。                      |
| ESLint                 | ESLint 帮助我们在多个贡献者的所有仓库中强制执行一致的编码风格。我们所有的 ESLint 规则都发布在 [eslint-plugin-adonis](https://github.com/adonisjs-community/eslint-plugin-adonis) 包下。                   |
| Prettier               | 我们使用 prettier 格式化代码库以获得一致的视觉输出。如果你对我们同时使用 ESLint 和 Prettier 感到困惑，请阅读 Prettier 网站上的 [Prettier vs. Linters](https://prettier.io/docs/en/comparison.html) 文档。 |
| EditorConfig           | 每个项目的根目录中的 `.editorconfig` 文件配置你的代码编辑器使用一组缩进和空白管理规则。同样，Prettier 用于后格式化你的代码，Editorconfig 用于提前配置编辑器。                                             |
| Conventional Changelog | 所有仓库的所有提交都使用 [commitlint](https://github.com/conventional-changelog/commitlint/#what-is-commitlint) 强制执行一致的提交消息。                                                                  |
| Husky                  | 我们使用 [husky](https://typicode.github.io/husky/#/) 在提交代码时强制执行提交约定。Husky 是一个用 Node 编写的 git 钩子系统                                                                               |

## 命令

| 命令                  | 描述                                                                                |
| --------------------- | ----------------------------------------------------------------------------------- |
| `npm run test`        | 使用 `ts-node` 运行项目测试                                                         |
| `npm run compile`     | 将 TypeScript 项目编译为 JavaScript。编译后的输出写入 `build` 目录                  |
| `npm run release`     | 使用 `np` 开始发布过程                                                              |
| `npm run lint`        | 使用 ESlint 检查代码库                                                              |
| `npm run format`      | 使用 Prettier 格式化代码库                                                          |
| `npm run sync-labels` | 将 `.github/labels.json` 文件中定义的标签与 Github 同步。此命令仅适用于项目管理员。 |

## 编码风格

我们所有的项目都使用 TypeScript 编写，并正在转向纯 ESM。

- 你可以 [在这里了解更多关于我的编码风格](https://github.com/thetutlage/meta/discussions/3)
- 查看我为 [ESM 和 TypeScript 遵循的设置](https://github.com/thetutlage/meta/discussions/2)

同时，确保在推送代码之前运行以下命令。

```sh
# 使用 prettier 格式化
npm run format

# 使用 Eslint 检查
npm run lint
```

## 获得贡献者认可

我们依赖 GitHub 在仓库的右侧面板中列出所有仓库贡献者。以下是相同的示例：

此外，我们使用 Github 的 [自动生成发布说明](https://docs.github.com/en/repositories/releasing-projects-on-github/automatically-generated-release-notes#about-automatically-generated-release-notes) 功能，该功能在发布说明中添加对贡献者个人资料的引用。
